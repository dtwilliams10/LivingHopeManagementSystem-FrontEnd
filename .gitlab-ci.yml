stages:
 - build
 - deploy

build:
  image: docker:latest
  stage: build
  script:
    - docker build -t lhms-app .

deploy: 
  image: 
    name: docker/compose:1.22.0
    entrypoint: ["/bin/sh", "-c"]
  stage: deploy
  script:
    - docker-compose -f "docker-compose.yml" up -e LETSENCRYPT_EMAIL=david.williams110913@gmail.com -e LETSENCRYPT_HOST=lhms.dtwilliams10.com -e VIRTUAL_HOST=lhms.dtwilliams10.com -d --build